version: '3.8'

services:
  dashboard:
    build: .
    ports:
      - '3001:80'
    environment:
      - VITE_API_URL=http://localhost:3000
    restart: unless-stopped
    networks:
      - cybersensei

networks:
  cybersensei:
    external: true

